{
  "name": "fastapi-mcp-inspector-build",
  "version": "1.0.0",
  "private": true,
  "description": "Build configuration for fastapi-mcp-inspector",
  "scripts": {
    "build:assets": "python -u scripts/build_assets.py"
  },
  "devDependencies": {
    "@modelcontextprotocol/inspector": "0.17.2"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=7.0.0"
  }
}
